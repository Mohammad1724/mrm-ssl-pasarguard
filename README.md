

# 🌟 جعبه‌ابزار مدیریت پاسارگارد (MRM Manager)

این اسکریپت یک دستیار هوشمند و همه‌کاره برای مدیران پنل **Pasarguard (مرزبان)** است. دیگر نیازی به حفظ کردن دستورات سخت لینوکس، ویرایش دستی فایل‌های کانفیگ یا دردسرهای گرفتن SSL ندارید. همه چیز با چند کلیک انجام می‌شود!

## 🚀 نصب و اجرا

فقط کافیست کد زیر را در ترمینال سرور خود کپی و اجرا کنید:

```bash
bash <(curl -Ls https://raw.githubusercontent.com/Mohammad1724/mrm-ssl-pasarguard/main/install.sh)
```

---

## 🛠️ امکانات و راهنمای منوها

بعد از اجرای دستور بالا، با منوی اصلی روبرو می‌شوید. در اینجا کارکرد هر گزینه را توضیح می‌دهیم:

### 1️⃣ مدیریت گواهینامه امنیتی (SSL Certificates Menu) 🔒
این بخش تمام نیازهای شما برای گرفتن و مدیریت SSL را برطرف می‌کند.

*   **Request New SSL (Wizard):** یک جادوگر هوشمند است! ابتدا دامنه و ایمیل شما را می‌گیرد، SSL را صادر می‌کند و سپس می‌پرسد این دامنه برای کجاست؟
    *   🔹 **Main Panel:** اگر برای دامنه اصلی پنل باشد، خودش فایل `.env` پنل را تنظیم کرده و پنل را ریستارت می‌کند.
    *   🟣 **Node Server:** اگر برای سرور نود باشد، فایل `.env` نود را تنظیم و نود را ریستارت می‌کند.
    *   🔸 **Config Domain:** اگر برای دامنه‌های اتصال (لینک‌های کانفیگ) باشد، فایل‌ها را در مسیر استاندارد ذخیره می‌کند و **آدرس دقیق** را به شما می‌دهد تا در پنل کپی کنید.
*   **Show Exact File Paths:** اگر فراموش کردید فایل‌های SSL کجا هستند، این گزینه آدرس دقیق تمام فایل‌های `crt` و `key` موجود را لیست می‌کند.
*   **List LetsEncrypt Certs:** لیست تمام دامنه‌هایی که تا به حال برایشان از Let's Encrypt گواهی گرفته‌اید.

---

### 2️⃣ مدیریت قالب (Theme Manager) 🎨
این بخش ظاهر پنل شما را از حالت ساده و خسته کننده، به یک قالب **مدرن، شیشه‌ای و نئونی** تبدیل می‌کند.

*   **Install / Update Theme:** نصب اولیه قالب. نام برند، آیدی ربات، متن اخبار و لینک‌های دانلود را از شما می‌پرسد و قالب را با **فرمت صحیح فارسی** نصب می‌کند.
*   **Activate Theme:** اگر قبلاً قالب را نصب کرده‌اید ولی غیرفعال شده، با این گزینه دوباره فعال می‌شود.
*   **Deactivate Theme:** اگر خواستید به ظاهر پیش‌فرض و اصلی پنل برگردید.
*   **Uninstall Theme:** حذف کامل فایل‌های قالب از سرور.

✨ **ویژگی‌های قالب:**
- طراحی شیشه‌ای (Glassmorphism)
- برند با افکت نئونی متحرک (Aurora Effect)
- نمودار مصرف هوشمند (میزان حجم و زمان)
- پشتیبانی کامل از حالت روز (Light) و شب (Dark)

---

### 3️⃣ تنظیمات و اتصال نود (Settings & Node Connector) ⚙️
این بخش عصای دست ادمین‌هاست!

*   **Connect Node to Core (Wizard) 🚀:** **(مهم‌ترین ویژگی)**
    *   دیگر نیازی نیست فایل `.env` نود را دستی بسازید!
    *   این گزینه آدرس سرور اصلی و پورت را از شما می‌گیرد.
    *   بررسی می‌کند که آیا SSL دارید یا نه.
    *   فایل کانفیگ نود را **خودکار می‌سازد** و نود را به سرور اصلی وصل می‌کند.
*   **Edit Panel Config:** فایل تنظیمات پنل اصلی (`.env`) را برای ویرایش باز می‌کند.
*   **Edit Node Config:** فایل تنظیمات نود (`.env`) را برای ویرایش باز می‌کند.
*   **Restart Main Panel:** ریستارت کردن سرویس پاسارگارد بدون نیاز به تایپ دستور داکر.
*   **Restart Node Service:** ریستارت کردن سرویس نود.

---

### 📦 پیش‌نیازها
- سیستم عامل: اوبونتو ۲۰.۰۴ به بالا (پیشنهادی)
- دسترسی روت (Root)
- نصب بودن پنل Pasarguard یا Node

---

### ❤️ حمایت
اگر این ابزار کار شما را راحت کرد، به این مخزن در گیت‌هاب **Star ⭐** بدهید