# 🔒 MRM SSL PASARGUARD v1.0

![Version](https://img.shields.io/badge/version-v1.0-blue)
![Language](https://img.shields.io/badge/language-Bash-green)
![License](https://img.shields.io/badge/license-MIT-orange)

**MRM SSL PASARGUARD** is an advanced, user-friendly Bash script designed to automate the generation, management, and renewal of SSL certificates (Let's Encrypt) for Linux servers. It is optimized for **Pasarguard Panel** but works perfectly for any environment.

---

## 🇮🇷 توضیحات فارسی

این اسکریپت یک ابزار مدیریت کامل برای دریافت و مدیریت گواهی SSL است. با استفاده از این ابزار می‌توانید بدون درگیر شدن با دستورات پیچیده، برای یک یا چندین دامنه به صورت همزمان SSL بگیرید، وضعیت اعتبار آن‌ها را چک کنید و فایل‌های خروجی را در مسیر دلخواه خود دریافت کنید.

### ✨ ویژگی‌های کلیدی
- ✅ **پشتیبانی از چند دامنه (Multi-Domain):** امکان دریافت SSL برای چندین دامنه به صورت همزمان و پشت سر هم.
- ↩️ **قابلیت بازگشت (Back):** امکان بازگشت به منوی قبلی در هر مرحله با تایپ `b` یا `back`.
- 🔄 **تمدید خودکار (Auto-Renewal):** تنظیم خودکار Cronjob برای کپی کردن فایل‌های جدید پس از تمدید توسط Let's Encrypt.
- 📂 **مسیردهی هوشمند:** ذخیره فایل‌ها در مسیر پیش‌فرض `/var/lib/pasarguard/certs` با پوشه‌بندی اختصاصی برای هر دامنه.
- 🧐 **استعلام وضعیت (Status Check):** مشاهده تاریخ انقضا، صادرکننده و وضعیت فعال بودن SSL روی سرور.
- 👁️ **نمایش کلیدها:** چاپ مستقیم محتوای `fullchain.pem` و `privkey.pem` در ترمینال برای کپی آسان.
- 🛡️ **بررسی امنیت:** چک کردن IP دامنه و سرور قبل از درخواست (جلوگیری از خطا در صورت روشن بودن کلادفلر).

### 🚀 نصب و اجرا (دستور تک خطی)
برای اجرای اسکریپت کافیست دستور زیر را در ترمینال سرور خود (با دسترسی روت) وارد کنید:

```bash
bash <(curl -Ls https://github.com/Mohammad1724/mrm-ssl-pasarguard/raw/main/install.sh)
